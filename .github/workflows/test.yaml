name: Runner Tests

on:
  pull_request:
    branches:
      - master
  push:
    branches:
      - master

jobs:
  validate-json-schema:
    runs-on: self-hosted
    steps:
    - uses: actions/checkout@v4
